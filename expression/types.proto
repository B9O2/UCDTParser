syntax = "proto3";
package ucdt;
option go_package = "./expression";

message SourceData{
    map<string, bytes> data = 1;
    string source = 2;
}

message SourceDataset{
    map<string,SourceData> dataset = 1;
}