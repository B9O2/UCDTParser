syntax = "proto3";
package ucdt;
option go_package = "./expression";

message SourceData{
    map<string, bytes> data = 1;
    string source = 2;
}

message SourceDataList{
    repeated SourceData sds = 1;
}